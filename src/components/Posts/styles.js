import { makeStyles } from '@mui/styles';

export default makeStyles(({
    mainContainer: {
        display: 'flex',
        alignItems: 'center',
    },
    smMargin: {
        margin: '1px',
    },
    actionDiv: {
        textAlign: 'center',
    },
}));